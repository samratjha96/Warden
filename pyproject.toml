[project]
name = "warden"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "anyio>=4.0.0",
    "langchain-openai>=0.1.0",
    "nvidia-nat==1.4.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
