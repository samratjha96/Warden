# NVIDIA API key for inference gateway (required for worker)
NVIDIA_API_KEY=your-nvidia-api-key-here

# Server configuration (optional, defaults shown)
# PORT=12000
# MAX_ACTIVE_JOBS=1
# SUBMIT_MIN_INTERVAL_SECONDS=1.0
# SUBMIT_WINDOW_SECONDS=60
# SUBMIT_MAX_PER_WINDOW=30
